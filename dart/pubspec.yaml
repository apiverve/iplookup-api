name: apiverve_iplookup
description: IP Lookup is a simple tool for looking up the location of an IP address. It returns the country, city, and more.
version: 1.1.14
homepage: https://iplookup.apiverve.com?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/iplookup-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
